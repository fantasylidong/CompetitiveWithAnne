# L4D2 AnneHappy Rework 更新記録

## 更新記録

### 2026年6月12日-7月3日 更新記録
#### Anne モードと特殊感染者スポーン
- Anne 系の対戦モードで第三者視点が使えない問題を修正しました。`!tp` と第三者視点関連 cvar は Anne 系共有設定から適用されます。
- `infected_control.smx` のリファクタリングと最適化を継続しました。Left4DHooks PVS/可視性補助、スポーン性能設定、キュー調整、ウェーブ判定整理を追加し、異常スポーン、Smoker の二重舌、補充ウェーブが速すぎる問題を減らしました。
- `anne_cvar_shield.smx` を追加し、Anne の重要 cvar を保護します。プラグインや投票の残留設定が別モードの難度へ混ざるのを防ぎます。
- 26-7 テスト設定 `Anne25-11.cfg` を追加し、旧版 `infected_control25-11.smx` をロールバック用として保持しました。動的難度ドキュメントも更新しました。
- 極限難度の値が他の難度へ影響する問題を修正しました。動的難度切り替え時に Tank、Hunter などの段階別パラメータをより明確にリセットします。
- セーフルーム内の近接武器が出ない場合がある問題を修正し、`MeleeInTheSafeRoom` の処理を整理しました。
- 刷特投票メニュー `spawn_vote_menu.smx` を従来の SourceMod メニューへ戻し、外部メニュー依存を削除しました。Anne/キャンペーンの特殊感染者数、間隔、自動モード、テレポート判定は項目を選ぶたびに単項目投票を開始し、プリセットは従来通り一括 preset 投票になります。
- not0721 系 coop/community/mutation モード、dirspawn preset、武器設定、SI limit 設定を追加・整理しました。
- `infected_control` に裏切り者モード関連ロジック、スポーン preset テーブル、多言語フレーズを追加し、今後の玩法拡張に備えました。

#### プレイヤー体験、投票、フィードバック
- `l4d_stats.smx` のカスタムマップのスコア精算を調整しました。通常のカスタムマップは実際のマップ時間が5分を超えた場合のみ安全部屋通過分と強勢通過ボーナスを付与し、公式マップは従来のスコアを維持します。カスタム救援マップにはチーム通過分を付与せず、マップ時間・マップ記録・特感統計は通常通り保存します。
- 旧 `hextags` を `hextags_lite.smx` に置き換え、称号色の問題を修正しました。管理者コマンドを隠し、設定ファイルは `hextags_lite.cfg` に移行しました。
- `global_chat.smx`、`join.smx` と関連メッセージに多言語フレーズを追加し、全体チャット状態やチーム募集受信状態などを補いました。
- `l4d_stats.smx` の四半期ランキングと永続化まわりを修正し、ラウンド状態記録を補強しました。一部のデータベース処理と混雑時ステータス書き込み負荷も下げました。
- 旧 `killsound` と「叮叮叮」音效投票の機能を `l4d2_hitsound.smx` に統合しました。古い `sound_on/off` 投票ファイルを削除し、命中、キル、ヘッドショットの音效とアイコンをフィードバック插件で一元管理します。
- `spechud.sp` を更新し、ping 表示、ボーナス割合の内訳表示、観戦遅延表示の修正を追加しました。
- `basevotes.smx` を無効化し、無蓄力 Hunter の直接投票項目を削除しました。Anne 動的難度と新しい投票メニューとの衝突を減らします。
- `server_name.smx` は SourceBans description を直接書き換えなくなりました。表示処理は外部 proxy 側へ移しました。
- 混雑時アンロードと NPC 管理のチェック/提示頻度を下げ、混雑時の追加負荷を抑えました。
- 一部の管理者コマンド出力を隠し、プレイヤー向けチャットのノイズを減らしました。

#### 上流同期、マップ、基礎依存
- Left4DHooks、gamedata、include、テスト用源码を更新し、新しい native/forward を補いました。関連插件も再ビルドしました。
- `ai_tank3.smx` を更新し、RPG スコア権限処理と極限難度の callback/パラメータ問題を修正しました。Tank 行動設定も調整しました。
- Dead Center 2025、City 17、No Echo m3、Carried Off `cwm1_intro` などの上流 map/stripper 修正を同期しました。適用できる zonemod 更新は `zonemod_anne` にも反映しました。
- `cwm1_intro` の hittable/clipwall 修正を同期し、複数モードの `mapinfo.txt` を補いました。
- Rust 製サーバーブラウザ工具と Docker デプロイ README をこの仓库から削除しました。関連ワークフローは外部工具/网页側で维护します。
- `basevotes.smx` の配置、SourceMod 設定、データベース字段、複数ドキュメントを更新し、插件包構造を整理しました。

### 2026年7月14日 動的難度ホットフィックス
- ハードコードされた `level0` が 1vHunters、Alone、WitchParty などのモード固有値を上書きする問題を修正しました。各モードの設定適用後に実際の CVar 基準値を記録し、難度変更、マップ変更、アンロード時に復元します。
- 低難度へ誤って追加された `z_lunge_up 0`、`z_lunge_interval 0.08`、`z_lunge_cooldown 0` などを削除し、旧 `ai_hunter_2` で Hunter がその場で飛ぶ、または繰り返ししゃがむ問題を修正しました。Jockey、Tank、Boomer への意図しない上書きも防止します。
- 極限と音理の明示的な強化値は維持し、高難度から離れる際は現在のモードで記録した基準値へ戻します。

### 2026年7月15日 動的難度修正
- 制御対象 CVar の基準値スキャンが後半の難度セクションを見落とす可能性を修正しました。`level1` から `level6` まで明示的に走査し、Expert 以下へ戻した際に Extreme または Neri 固有値が残らないようにしました。
- 動的難度で視点上げ/旋回が有効なとき、旧 `ai_boomer_2` が下向きの俯角を残して地面へ吐くことがある問題を修正しました。初回照準と複数目標への旋回は最低 5 度の仰角を維持し、連続ジャンプやジャンプ吐きの設定は変更しません。
- 使用中の AI プラグインに登録されていない旧 CVar 4 個を削除しました。SourceMod から取得・変更できる hidden/launcher CVar 3 個は設定を維持します。
- 固定難度から自動へ戻す際、PPM データが一時的に利用できないと Neri/高難度 CVar が残る問題を修正しました。まず Easy を安全な基準として適用し、統計が利用可能になった時点で自動難度を再選択します。
- サーバーコンソール/RCON の `sm_aidiff` までセーフルーム退出後のロックで次ラウンドへ延期され、運用上の難度変更が未適用に見える問題を修正しました。コンソール/RCON は即時適用し、退出後にプレイヤーが実行した管理者コマンドは従来どおり次ラウンドから適用します。
- Boomer2 と Smoker3 に、既定で無効の空中能力 CVar を追加しました。Neri のみ有効化し、既存の連続ジャンプ動作は従来どおり継続します。追加のジャンプを強制せず、すでに空中にいる AI が吐しゃまたは舌を使用できるようにするだけで、地上攻撃と発動距離は変更しません。

### 2026年7月16日 新規プレイヤーのダウンロード最適化
- `l4d2_blackscreen_fix.smx` の FastDL 配信を再構成しました。初回接続では命中フィードバック用アイコンとゲーム内蔵ではないフィードバック音を優先し、ダンス素材は実際のマップ切り替え時に段階的に補います。
- `restrict_strings.cfg` は初回接続用の優先リストとなり、44 個のフィードバック素材ファイルと重複を除いた 17 個のフィードバック音声ファイルを含みます。ダンスモデルは初回接続時にはダウンロードされません。
- 60 曲のダンス曲を管理する `deferred_strings.cfg` を追加し、既定では 8 グループに分割しました。実際の `changelevel` ごとにランダムな 1 グループを配信し、8 グループがすべて出るまでは同じグループを繰り返しません。一巡後は再度シャッフルします。`sm_fixscreen_deferred_group_count` でグループ数を変更でき、クライアント側にあるファイルはゲームのキャッシュにより自動的にスキップされます。
- 相互依存する 3 個のダンスモデルファイルは、最初のマップ切り替え時にまとめてダウンロードし、不完全なモデルによるエラーを防ぎます。同じ切り替えでランダムな曲グループも 1 つ配信し、その後はランダムかつ重複しない順序で補います。
- フィードバック、モデル、ダンス曲の全 124 リソースを元のディレクトリ構造を保った `.bz2` として NewAnneWeb のファイル管理へ移行しました。FastDL は Cloudflare キャッシュを利用する `http://anne.trygek.com/fastdl/left4dead2` へ切り替えました。

### 2026年7月22日 動的難度 PPM 修正
- 自動難度が `round_start` の時点でチーム PPM を早くロックしすぎ、その後観戦へ移ったプレイヤーが当該ラウンドの難度計算に残る問題を修正しました。
- ラウンド開始時の難度は暫定値となります。セーフルーム内で生存者メンバーが変わると暫定 PPM を無効化し、最初の生存者が退出した時点で生存者チームに残っている実プレイヤーだけを再計算してロックします。観戦者と Bot は対象外です。

### 2026年7月23日 FastDL マップ切り替え安定性修正
- `l4d2_blackscreen_fix.smx` から非公開エンジン関数 `CNetworkStringTable::DeleteAllStrings()` の呼び出しを削除し、マップ読み込み中に `downloadables` テーブルを消去してサーバーがクラッシュまたは watchdog により再起動される可能性をなくしました。
- ダウンロード配信を追加専用に変更しました。各マップでは優先フィードバック素材のみを追加し、実際のマップ切り替え時に SourceMod 公開 API `AddFileToDownloadsTable()` で 3 個のダンスモデルとランダムな曲グループ 1 組を追加します。文字列テーブルの削除、上書き、復元は行いません。
- `fornite_l4d.smx` は従来の全ダンス素材ダウンロード登録を、新しい既定無効の `sm_emotes_add_downloads` スイッチの内側に保持します。このサーバーではグローバルに読み込まれる `l4d2_blackscreen_fix.smx` が段階的ダウンロードを管理し、他のサーバーではスイッチを有効にすると従来の一括ダウンロードへ戻せます。

### 2026年7月26日 マップ記録 v2
- `l4d_stats.smx` に汎用 `ruleset_key`、ルールセットバージョン、記録ポリシー、計時/難易度プロファイルを追加しました。Anne 系はベストタイムランキングを維持し、Co-op/Survival はルールごとに分離し、Versus、Realism Versus、Scavenge は最速比較ではなくラウンド履歴として保存します。
- `map_runs`、`map_run_players`、`legacy_map_bests` と、再実行可能な移行/ロールバックスクリプトを追加しました。旧 `timedmaps`/`timedmap_runs` は保持し、旧版のプレイヤー別ベストと実際のランを分けて扱います。
- NewAnneWeb のプレイヤーページ、マップ一覧、詳細ページを v2 に対応し、ルール、バージョン、難易度/AI レベル、チーム人数、Anne スポーン設定で絞り込めます。導入手順は `docs/map_records_v2_migration.md` を参照してください。

### 2026年8月10日 Boomer/Spitter Path Follow 連続ジャンプ
- ネイティブ拡張 `anne_nextbot` を 1.3.0 へ更新しました。唯一の `PathFollower::Update` detour として、上限付きの経路ノード値をネイティブ側で直接コピーします。Tank、Charger、Boomer、Spitter は native 経由でスナップショットを読み取り、SourcePawn は PathFollower/PathSegment ポインタを受け取らず、読み取りもしません。共通の経路シグネチャと仮想関数オフセットは `anne_nextbot.games.txt` に集約しました。
- Tank `ai_tank3` を 1.0.0.4、Charger `ai_charger3` を 1.0.1.4 に更新し、既存の Path Follow 連続ジャンプを同じ native スナップショットへ移行しました。各 gamedata には爪攻撃、突進、locomotion など固有の呼び出しだけを残し、共通 PathFollower 定義の重複を削除しました。
- Boomer を `ai_boomer_3.smx` 3.0.4、Spitter を `ai_spitter_3.smx` 3.0.2 へ更新しました。連続ジャンプは有効な経路先読み地点を優先し、経路が古い、遮られている、または前方に登り・隙間ジャンプなどの特殊移動がある場合は従来の AI/直線動作へ戻ります。
- 4 つの AI は、滞空時間予測、壁・着地点・局所的な段差の安全確認、段階的な空中速度補正を `ai_path_movement.inc` で共有します。Charger 固有の突進、再経路探索、横方向の駆け引きは従来どおり状態機械が担当します。
- Boomer は一時的に直接視認できない曲がり角でも、有効な PathFollower 経路があれば接近を続けます。26-07 AI2 の吐しゃ角度修正も維持しています。
- 現行 2026-08 の AnneHappy、Hardcore、Shotgun は AI3 を読み込みます。`Anne26-07.cfg` は未変更の `ai_boomer_2.smx` と `ai_spitter_2.smx` を引き続き読み込み、26-07 ロールバック版として保持します。

### 2026年8月13日 infected_control 監査修正
- `infected_control.smx` を 2026-08-13.1 へ更新し、全体コード監査で見つかったカウント・状態の問題を修正しました。ウェーブ時系列の契約（キルフェーズ、独立した 16 秒基礎カウントダウン、上限なしの Anti-Bait ホールド）の挙動は変わりません。
- 特殊感染者の生存数二重カウントを修正：`RecalcSiCapFromAlive` などの全体再集計は「検証待ち・拒否されキック待ち・キックキュー内」の一時的なクライアントをスキップするようになり、テレポートキックや確認待ちスポーン中に職業/合計上限が先に埋まってウェーブ判断が歪む問題を防ぎます。
- 動的 Nav トポロジー（エレベーター等）再構築後に進捗エクスポートが失敗すると、スポーン探索が永遠に Pending のまま止まるデッドロックを修正：待機フラグを解除して旧 Flow バケットへフォールバックし、ディレクター代替も復旧します。
- マップ間のキャッシュ汚染を修正：生存者進捗のフォールバック値とバケット別生存特感カウントのキャッシュをラウンド/マップ切替時にリセットし、新マップで `GetGameTime` が戻っても前マップのデータを使い続けないようにしました。バケット構築のたびに漏れていた 2 つの ArrayList ハンドルも解放します。
- Nav バケットのディスクキャッシュを堅牢化：メタデータは一致しても有効な区域が 1 つもない破損キャッシュは削除して再構築し、エクスポート失敗時は書きかけのファイルを残さず削除します。
- テレポート監視の 2 つの問題を修正：出生時刻が不明な場合を永続的な出生猶予として扱わなくなり（ウォッチドッグのハード復旧後に引き継いだ特感へ出生時刻を再付与し、正常にテレポートできます）、Smoker は能力クールダウン中も不可視カウントを蓄積し、準備完了後に秒数を貯め直す必要がなくなりました。
- 最終着地検証で古い視点スナップショットのまま距離のハード判定を行う順序問題を修正。`GetNextSpawnTime` native はキルフェーズ中に「残り確認ウィンドウ + 完全な基礎カウントダウン」の上限見積もりを返し、ポーズ中も凍結した時計で見積もりを続け、キルフェーズの時間を基礎カウントダウンに算入しません。
- Anti-Bait 状態機の再設計（2026-08-13.2）：散開によるホールド解除は継続確認が必要になりました。新しい `inf_antibait_spread_confirm`（既定 3 秒）の間、隊形が連続して散開し続けない限り Pressure は降格しません。「少し離れてすぐ戻る」フェイクではホールドを騙せず、散開は停滞タイマーもリセットしません。武装ウィンドウ内で再集結し依然停滞しているチームは、同一ホールドの継続として直接 Pressure に復帰します。
- Anti-Bait 進行度スナップショットの分離：生存者の flow 読み取り失敗（エレベーター、不正 Nav）を「脆弱メンバー」として扱わなくなり、ホールドを誤って阻止/解除しません。該当メンバーは隊形統計には通常どおり参加し、進行度統計からのみ除外されます。flow が回復したら基線を再構築しますが、停滞タイマーはリセットしません。
- 内通者（Traitor）の実体化は `totalSI` 上限の制限を受けなくなりました。放出ウェーブが到来した SpawnAllowed 状態の内通者（枠は開波時にそのウェーブの AI 予算から控除済み）は、残存 AI が一時的に上限を埋めていても実体化できます。超過中は AI の補充が自動的に停止します。
- `create_panic_event` は 60 秒のローリングウィンドウになりました。新しい panic イベントを受け取るたびに更新し、`panic_event_finished` で即座にクリアします。終了イベントを送らないカスタムマップが Anti-Bait をラウンド全体で静かにバイパスするのを防ぎ、連続/無限ホードスクリプトはウィンドウを更新し続けるため影響を受けません。
- `sm_wavestatus` と Nav デバッグコマンドの応答が翻訳フレーズ（簡中/繁中/英/日/韓/越の 12 フレーズを追加）を使うようになり、中国語のハードコードを廃止しました。
- 早期カウントダウン開始の「生存者が健康 + Tank なし」という慈悲ゲートは意図的な設計であることを確認し、AGENTS.md の行動契約に記載しました（散開確認と内通者免除の仕様も併記）。
