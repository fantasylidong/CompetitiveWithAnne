# L4D2 AnneHappy Rework 更新記録

## 更新記録

### 2026年6月12日-7月3日 更新記録
#### Anne モードと特殊感染者スポーン
- Anne 系の対戦モードで第三者視点が使えない問題を修正しました。`!tp` と第三者視点関連 cvar は Anne 系共有設定から適用されます。
- `infected_control.smx` のリファクタリングと最適化を継続しました。Left4DHooks PVS/可視性補助、スポーン性能設定、キュー調整、ウェーブ判定整理を追加し、異常スポーン、Smoker の二重舌、補充ウェーブが速すぎる問題を減らしました。
- `anne_cvar_shield.smx` を追加し、Anne の重要 cvar を保護します。プラグインや投票の残留設定が別モードの難度へ混ざるのを防ぎます。
- 26-7 テスト設定 `Anne25-11.cfg` を追加し、旧版 `infected_control25-11.smx` をロールバック用として保持しました。動的難度ドキュメントも更新しました。
- 極限難度の値が他の難度へ影響する問題を修正しました。動的難度切り替え時に Tank、Hunter などの段階別パラメータをより明確にリセットします。
- セーフルーム内の近接武器が出ない場合がある問題を修正し、`MeleeInTheSafeRoom` の処理を整理しました。
- 刷特投票メニュー `spawn_vote_menu.smx` を従来の SourceMod メニューへ戻し、外部メニュー依存を削除しました。Anne/キャンペーンの特殊感染者数、間隔、自動モード、テレポート判定は項目を選ぶたびに単項目投票を開始し、プリセットは従来通り一括 preset 投票になります。
- not0721 系 coop/community/mutation モード、dirspawn preset、武器設定、SI limit 設定を追加・整理しました。
- `infected_control` に裏切り者モード関連ロジック、スポーン preset テーブル、多言語フレーズを追加し、今後の玩法拡張に備えました。

#### プレイヤー体験、投票、フィードバック
- `l4d_stats.smx` のカスタムマップのスコア精算を調整しました。通常のカスタムマップは実際のマップ時間が5分を超えた場合のみ安全部屋通過分と強勢通過ボーナスを付与し、公式マップは従来のスコアを維持します。カスタム救援マップにはチーム通過分を付与せず、マップ時間・マップ記録・特感統計は通常通り保存します。
- 旧 `hextags` を `hextags_lite.smx` に置き換え、称号色の問題を修正しました。管理者コマンドを隠し、設定ファイルは `hextags_lite.cfg` に移行しました。
- `global_chat.smx`、`join.smx` と関連メッセージに多言語フレーズを追加し、全体チャット状態やチーム募集受信状態などを補いました。
- `l4d_stats.smx` の四半期ランキングと永続化まわりを修正し、ラウンド状態記録を補強しました。一部のデータベース処理と混雑時ステータス書き込み負荷も下げました。
- 旧 `killsound` と「叮叮叮」音效投票の機能を `l4d2_hitsound.smx` に統合しました。古い `sound_on/off` 投票ファイルを削除し、命中、キル、ヘッドショットの音效とアイコンをフィードバック插件で一元管理します。
- `spechud.sp` を更新し、ping 表示、ボーナス割合の内訳表示、観戦遅延表示の修正を追加しました。
- `basevotes.smx` を無効化し、無蓄力 Hunter の直接投票項目を削除しました。Anne 動的難度と新しい投票メニューとの衝突を減らします。
- `server_name.smx` は SourceBans description を直接書き換えなくなりました。表示処理は外部 proxy 側へ移しました。
- 混雑時アンロードと NPC 管理のチェック/提示頻度を下げ、混雑時の追加負荷を抑えました。
- 一部の管理者コマンド出力を隠し、プレイヤー向けチャットのノイズを減らしました。

#### 上流同期、マップ、基礎依存
- Left4DHooks、gamedata、include、テスト用源码を更新し、新しい native/forward を補いました。関連插件も再ビルドしました。
- `ai_tank3.smx` を更新し、RPG スコア権限処理と極限難度の callback/パラメータ問題を修正しました。Tank 行動設定も調整しました。
- Dead Center 2025、City 17、No Echo m3、Carried Off `cwm1_intro` などの上流 map/stripper 修正を同期しました。適用できる zonemod 更新は `zonemod_anne` にも反映しました。
- `cwm1_intro` の hittable/clipwall 修正を同期し、複数モードの `mapinfo.txt` を補いました。
- Rust 製サーバーブラウザ工具と Docker デプロイ README をこの仓库から削除しました。関連ワークフローは外部工具/网页側で维护します。
- `basevotes.smx` の配置、SourceMod 設定、データベース字段、複数ドキュメントを更新し、插件包構造を整理しました。

### 2026年7月14日 動的難度ホットフィックス
- ハードコードされた `level0` が 1vHunters、Alone、WitchParty などのモード固有値を上書きする問題を修正しました。各モードの設定適用後に実際の CVar 基準値を記録し、難度変更、マップ変更、アンロード時に復元します。
- 低難度へ誤って追加された `z_lunge_up 0`、`z_lunge_interval 0.08`、`z_lunge_cooldown 0` などを削除し、旧 `ai_hunter_2` で Hunter がその場で飛ぶ、または繰り返ししゃがむ問題を修正しました。Jockey、Tank、Boomer への意図しない上書きも防止します。
- 極限と音理の明示的な強化値は維持し、高難度から離れる際は現在のモードで記録した基準値へ戻します。

### 2026年7月15日 動的難度修正
- 制御対象 CVar の基準値スキャンが後半の難度セクションを見落とす可能性を修正しました。`level1` から `level6` まで明示的に走査し、Expert 以下へ戻した際に Extreme または Neri 固有値が残らないようにしました。
- 動的難度で視点上げ/旋回が有効なとき、旧 `ai_boomer_2` が下向きの俯角を残して地面へ吐くことがある問題を修正しました。初回照準と複数目標への旋回は最低 5 度の仰角を維持し、連続ジャンプやジャンプ吐きの設定は変更しません。
- 使用中の AI プラグインに登録されていない旧 CVar 4 個を削除しました。SourceMod から取得・変更できる hidden/launcher CVar 3 個は設定を維持します。
- 固定難度から自動へ戻す際、PPM データが一時的に利用できないと Neri/高難度 CVar が残る問題を修正しました。まず Easy を安全な基準として適用し、統計が利用可能になった時点で自動難度を再選択します。
- サーバーコンソール/RCON の `sm_aidiff` までセーフルーム退出後のロックで次ラウンドへ延期され、運用上の難度変更が未適用に見える問題を修正しました。コンソール/RCON は即時適用し、退出後にプレイヤーが実行した管理者コマンドは従来どおり次ラウンドから適用します。
- Boomer2 と Smoker3 に、既定で無効の空中能力 CVar を追加しました。Neri のみ有効化し、既存の連続ジャンプ動作は従来どおり継続します。追加のジャンプを強制せず、すでに空中にいる AI が吐しゃまたは舌を使用できるようにするだけで、地上攻撃と発動距離は変更しません。

### 2026年7月16日 新規プレイヤーのダウンロード最適化
- `l4d2_blackscreen_fix.smx` の FastDL 配信を再構成しました。初回接続では命中フィードバック用アイコンとゲーム内蔵ではないフィードバック音を優先し、ダンス素材は実際のマップ切り替え時に段階的に補います。
- `restrict_strings.cfg` は初回接続用の優先リストとなり、44 個のフィードバック素材ファイルと重複を除いた 17 個のフィードバック音声ファイルを含みます。ダンスモデルは初回接続時にはダウンロードされません。
- 60 曲のダンス曲を管理する `deferred_strings.cfg` を追加し、既定では 8 グループに分割しました。実際の `changelevel` ごとにランダムな 1 グループを配信し、8 グループがすべて出るまでは同じグループを繰り返しません。一巡後は再度シャッフルします。`sm_fixscreen_deferred_group_count` でグループ数を変更でき、クライアント側にあるファイルはゲームのキャッシュにより自動的にスキップされます。
- 相互依存する 3 個のダンスモデルファイルは、最初のマップ切り替え時にまとめてダウンロードし、不完全なモデルによるエラーを防ぎます。同じ切り替えでランダムな曲グループも 1 つ配信し、その後はランダムかつ重複しない順序で補います。
- フィードバック、モデル、ダンス曲の全 124 リソースを元のディレクトリ構造を保った `.bz2` として NewAnneWeb のファイル管理へ移行しました。FastDL は Cloudflare キャッシュを利用する `http://anne.trygek.com/fastdl/left4dead2` へ切り替えました。

### 2026年7月22日 動的難度 PPM 修正
- 自動難度が `round_start` の時点でチーム PPM を早くロックしすぎ、その後観戦へ移ったプレイヤーが当該ラウンドの難度計算に残る問題を修正しました。
- ラウンド開始時の難度は暫定値となります。セーフルーム内で生存者メンバーが変わると暫定 PPM を無効化し、最初の生存者が退出した時点で生存者チームに残っている実プレイヤーだけを再計算してロックします。観戦者と Bot は対象外です。

### 2026年7月23日 FastDL マップ切り替え安定性修正
- `l4d2_blackscreen_fix.smx` から非公開エンジン関数 `CNetworkStringTable::DeleteAllStrings()` の呼び出しを削除し、マップ読み込み中に `downloadables` テーブルを消去してサーバーがクラッシュまたは watchdog により再起動される可能性をなくしました。
- ダウンロード配信を追加専用に変更しました。各マップでは優先フィードバック素材のみを追加し、実際のマップ切り替え時に SourceMod 公開 API `AddFileToDownloadsTable()` で 3 個のダンスモデルとランダムな曲グループ 1 組を追加します。文字列テーブルの削除、上書き、復元は行いません。
- `fornite_l4d.smx` は従来の全ダンス素材ダウンロード登録を、新しい既定無効の `sm_emotes_add_downloads` スイッチの内側に保持します。このサーバーではグローバルに読み込まれる `l4d2_blackscreen_fix.smx` が段階的ダウンロードを管理し、他のサーバーではスイッチを有効にすると従来の一括ダウンロードへ戻せます。

### 2026年7月26日 マップ記録 v2
- `l4d_stats.smx` に汎用 `ruleset_key`、ルールセットバージョン、記録ポリシー、計時/難易度プロファイルを追加しました。Anne 系はベストタイムランキングを維持し、Co-op/Survival はルールごとに分離し、Versus、Realism Versus、Scavenge は最速比較ではなくラウンド履歴として保存します。
- `map_runs`、`map_run_players`、`legacy_map_bests` と、再実行可能な移行/ロールバックスクリプトを追加しました。旧 `timedmaps`/`timedmap_runs` は保持し、旧版のプレイヤー別ベストと実際のランを分けて扱います。
- NewAnneWeb のプレイヤーページ、マップ一覧、詳細ページを v2 に対応し、ルール、バージョン、難易度/AI レベル、チーム人数、Anne スポーン設定で絞り込めます。導入手順は `docs/map_records_v2_migration.md` を参照してください。

### 2026年8月10日 Boomer/Spitter Path Follow 連続ジャンプ
- ネイティブ拡張 `anne_nextbot` を 1.3.0 へ更新しました。唯一の `PathFollower::Update` detour として、上限付きの経路ノード値をネイティブ側で直接コピーします。Tank、Charger、Boomer、Spitter は native 経由でスナップショットを読み取り、SourcePawn は PathFollower/PathSegment ポインタを受け取らず、読み取りもしません。共通の経路シグネチャと仮想関数オフセットは `anne_nextbot.games.txt` に集約しました。
- Tank `ai_tank3` を 1.0.0.4、Charger `ai_charger3` を 1.0.1.4 に更新し、既存の Path Follow 連続ジャンプを同じ native スナップショットへ移行しました。各 gamedata には爪攻撃、突進、locomotion など固有の呼び出しだけを残し、共通 PathFollower 定義の重複を削除しました。
- Boomer を `ai_boomer_3.smx` 3.0.4、Spitter を `ai_spitter_3.smx` 3.0.2 へ更新しました。連続ジャンプは有効な経路先読み地点を優先し、経路が古い、遮られている、または前方に登り・隙間ジャンプなどの特殊移動がある場合は従来の AI/直線動作へ戻ります。
- 4 つの AI は、滞空時間予測、壁・着地点・局所的な段差の安全確認、段階的な空中速度補正を `ai_path_movement.inc` で共有します。Charger 固有の突進、再経路探索、横方向の駆け引きは従来どおり状態機械が担当します。
- Boomer は一時的に直接視認できない曲がり角でも、有効な PathFollower 経路があれば接近を続けます。26-07 AI2 の吐しゃ角度修正も維持しています。
- 現行 2026-08 の AnneHappy、Hardcore、Shotgun は AI3 を読み込みます。`Anne26-07.cfg` は未変更の `ai_boomer_2.smx` と `ai_spitter_2.smx` を引き続き読み込み、26-07 ロールバック版として保持します。

### 2026年8月13日 infected_control 監査修正
- `infected_control.smx` を 2026-08-13.2 へ更新し、全体コード監査で見つかったカウント・状態の問題を修正しました。ウェーブ時系列の契約（キルフェーズ、独立した 16 秒基礎カウントダウン、上限なしの Anti-Bait ホールド）の挙動は変わりません。
- 特殊感染者の生存数二重カウントを修正：`RecalcSiCapFromAlive` などの全体再集計はキックキュー内の一時的なクライアントをスキップするようになり、テレポートキック中に職業/合計上限が先に埋まってウェーブ判断が歪む問題を防ぎます。
- 動的 Nav トポロジー（エレベーター等）再構築後に進捗エクスポートが失敗すると、スポーン探索が永遠に Pending のまま止まるデッドロックを修正：待機フラグを解除して旧 Flow バケットへフォールバックし、ディレクター代替も復旧します。
- マップ間のキャッシュ汚染を修正：生存者進捗のフォールバック値とバケット別生存特感カウントのキャッシュをラウンド/マップ切替時にリセットし、新マップで `GetGameTime` が戻っても前マップのデータを使い続けないようにしました。バケット構築のたびに漏れていた 2 つの ArrayList ハンドルも解放します。
- Nav バケットのディスクキャッシュを堅牢化：メタデータは一致しても有効な区域が 1 つもない破損キャッシュは削除して再構築し、エクスポート失敗時は書きかけのファイルを残さず削除します。
- テレポート監視の 2 つの問題を修正：出生時刻が不明な場合を永続的な出生猶予として扱わなくなり（ウォッチドッグのハード復旧後に引き継いだ特感へ出生時刻を再付与し、正常にテレポートできます）、Smoker は能力クールダウン中も不可視カウントを蓄積し、準備完了後に秒数を貯め直す必要がなくなりました。
- `GetNextSpawnTime` native はキルフェーズ中に「残り確認ウィンドウ + 完全な基礎カウントダウン」の上限見積もりを返し、ポーズ中も凍結した時計で見積もりを続け、キルフェーズの時間を基礎カウントダウンに算入しません。
- Anti-Bait 状態機の再設計：散開によるホールド解除は継続確認が必要になりました。新しい `inf_antibait_spread_confirm`（既定 3 秒）の間、隊形が連続して散開し続けない限り Pressure は降格しません。「少し離れてすぐ戻る」フェイクではホールドを騙せず、散開は停滞タイマーもリセットしません。武装ウィンドウ内で再集結し依然停滞しているチームは、同一ホールドの継続として直接 Pressure に復帰します。
- Anti-Bait 進行度スナップショットの分離：生存者の flow 読み取り失敗（エレベーター、不正 Nav）を「脆弱メンバー」として扱わなくなり、ホールドを誤って阻止/解除しません。該当メンバーは隊形統計には通常どおり参加し、進行度統計からのみ除外されます。flow が回復したら基線を再構築しますが、停滞タイマーはリセットしません。
- 内通者（Traitor）の実体化は `totalSI` 上限の制限を受けなくなりました。放出ウェーブが到来した SpawnAllowed 状態の内通者（枠は開波時にそのウェーブの AI 予算から控除済み）は、残存 AI が一時的に上限を埋めていても実体化できます。超過中は AI の補充が自動的に停止します。
- `create_panic_event` は 60 秒のローリングウィンドウになりました。新しい panic イベントを受け取るたびに更新し、`panic_event_finished` で即座にクリアします。終了イベントを送らないカスタムマップが Anti-Bait をラウンド全体で静かにバイパスするのを防ぎ、連続/無限ホードスクリプトはウィンドウを更新し続けるため影響を受けません。
- `sm_wavestatus` と Nav デバッグコマンドの応答が翻訳フレーズ（簡中/繁中/英/日/韓/越の 12 フレーズを追加）を使うようになり、中国語のハードコードを廃止しました。
- 早期カウントダウン開始の「生存者が健康 + Tank なし」という慈悲ゲートは意図的な設計であることを確認し、AGENTS.md の行動契約に記載しました（散開確認と内通者免除の仕様も併記）。

### 2026年8月13日 AI3 プラグイン監査修正
- `ai_tank3` を 1.0.0.8 へ更新：`OnPlayerRunCmd` が各サブロジックの戻り値を集約して返すようになり、投石距離制限、頭上ブロック対策の強制投石、連続ジャンプのキー変更がエンジンに捨てられる問題を修正しました。パンチ視点ロックは GameTime 時計に統一し、投石の出手高さはアニメーション活動名から解決し、`tank_swing_range` の変更フックはマップ切替ごとに重複登録されなくなりました。
- `ai_charger3` を 1.0.1.6 へ更新：状態機械 `transitionTo` が旧状態の `OnExit` を実行しなかった問題を修正（静的構造体を読み込む前に判定していた）。空中急停止、後跳、人為的な突進遅延フラグが状態切替時に正しくリセットされます。`player_spawn` は Post フックになり、梯子上でジャンプ/しゃがみ入力を消した後は正しく `Plugin_Changed` を返します。
- `ai_boomer_3` を 3.0.6 へ更新：`L4D_OnVomitedUpon` が attacker を検証し、胆汁弾などで attacker が 0 のたびにエラーが出る問題を修正。既存の転噴キューがあるときは再構築せず、強制胆汁の再帰による目標重複膨張を防ぎます。被害者選択の早期 return での trace ハンドル漏れと、強制胆汁ループの誤インデックスも修正。最近の生存者検索は 0.2 秒キャッシュして毎フレームのソート負荷を下げます。
- `ai_spitter_3` を 3.0.4 へ更新：人混み目標がピン留め対象の座標を誤用していたコピーペースト（-1 インデックスエラー含む）を修正。`getTargetByPriority` がピン留め中または生存者 1 人のときに ArrayList を漏らす問題を修正。生存者が 1 人でも flow で選べるようにし、群衆距離累積セルを明示的にゼロ初期化し、常に偽になるレイフィルタの死コードを削除しました。
- レイコスト最適化：`ai_path_movement.inc` にジャンプ経路失敗のスロットルを追加。地形（壁、段差、危険な着地点）で失敗した安全確認は 0.1 秒冷却中に再試行しません。4 つの AI の地上ジャンプ試行がこれを使い、地形に引っかかっているときの hull/ray trace コストを約 1/6（60 tick）に下げます。`AIPathMovement_IsJumpRouteSafe` はレイ不要の無視界角度拒否を hull trace より前に移し、拒否された試行は一切トレースを出しません。

### 2026年8月13日 ヒットフィードバック音セットのカスタマイズ
- `l4d2_hitsound.smx` の音セットに「いつ鳴るか」を定義する 2 つのオプションキーを追加：`headshot_kill` はヘッドショットキル専用の音を指定（空 = headshot 音へフォールバック、従来動作）。`stack` はそのセットのヒット/キルイベントを毎回即座に個別再生するか（同フレームで重ねられるか）を制御します。`stack` の既定値は 1（スタック）で、明示的に `stack 0` と書いた場合のみ「1 フレーム 1 音、キル＞ヘッドショット＞ヒット」の優先度マージが有効になります。
- セット 9「元の叮投票プラグイン」はこれで旧 killsound（Dingshot）の手触りを再現：ヘッドショット命中＝チン（littlereward）、ヘッドショットキル＝ベル（bell_normal）、非ヘッドショットの命中/キルは無音。ショットガンの複数キルはキルごとに鳴る挙動に戻ります。
- `!snd` メインメニューに全プレイヤー向けの個人再生設定を 2 つ追加（サーバーごとの KV ファイルに保存、データベースには入れません）：「ヘッドショットキル専用音 オン/オフ」（オフ = 通常のヘッドショット音にフォールバック）と、3 状態の「効果音再生モード」（セットに従う / 強制スタック / 強制マージ）。切替時の応答は各モードの意味を説明する翻訳フレーズ（簡中/繁中/英/日/韓/越の 5 フレーズを追加）を使います。
### 2026年8月13日 ネットワーク品質レポート
- `network_quality_hint.smx` を 1.1.4 へ更新。プラグインの ConVar 既定値は引き続き `nqh_report_enable "0"` で、ローカルの ping/loss/choke 検出はレポートに依存しません。本リポジトリはウェブサイトのホワイトリスト内 Anne ゲームサーバー向けに `cfg/sourcemod/network_quality_hint.cfg` で `nqh_report_enable "1"` を明示します。
- レポートには SteamWorks が必要です。SteamWorks が無い場合でもプラグインはロードされローカル通知は出ますが、`nqh_report_url` へは送信しません。
- ウェブサイトのホワイトリスト外のコミュニティサーバーは `nqh_report_enable "0"` のままにしてください。
### 2026年8月13日 帽子/スキン保存修正
- `l4d_hats.smx` を 1.46.1 へ更新：`Hats_SetClientHat` / `Hats_GetClientHat` native を追加し、RPG は `sm_hatclient` コンソールコマンドを使わずに帽子を復元できます。
- 帽子を外したとき `L4D_OnHatLoadSave` は `-1` を返すようになり、最初の帽子の 0 番インデックスと衝突しません。Cookie 削除と自動装着は帽子メニュー権限（上位 100 を含む）に合わせ、ランキングキャッシュ待ちで保存済み帽子を消さないようにしました。
- `rpg.smx` を 2.0.1 へ更新：データベース読み込み完了後に帽子・スキン・輪郭を再適用し、ランキング未準備のまま帽子/スキンを 0 にクリアしなくなりました。
- 最初の帽子は `-2` で保存し、`HAT=0` が「未装着」と「1 番目の帽子」の両方を意味しないようにしました。旧データの読み込み前に帽子を選んでも上書きされません。
- スキンにも同じ「非同期 DB 読み込み後に復元されない / ランキング未準備でクリアされる」問題があり、まとめて修正しました。称号、反動、ダメージ HUD、ヒット音は各自の読み込み完了コールバックまたは独立した Cookie/DB で復元しており、同様の穴はありません。

### 2026年8月13日 全員初心者チームボーナス
- `l4d_stats.smx` を 1.5.6 へ更新。場の人間生存者が全員初心者で 4 人以上のとき、全員が 1.5 倍の新人チームボーナスを受け取ります。従来は「初心者本人には倍率なし」のため誰も受け取れませんでした。
- 初心者が 1～3 人のときは従来どおり、経験者だけが 1.1 / 1.3 / 1.5 を受け取ります。
- `l4d_stats_newbie_bonus_all_newbie_multiplier`（既定 1.5）と `l4d_stats_newbie_bonus_all_newbie_min`（既定 4）を追加。セーフルーム、フィナーレ、ラウンド開始のチャットは各言語でチームボーナスを説明します。
### 2026年8月13日 text プラグイン状態表示の翻訳
- `optional/AnneHappy/text.smx` の `!xx` / 入室 / ラウンド開始の状態行をクライアント言語で出すようにし、英語プレイヤーに「特感、連跳、動態難度、回血」などの中国語ハードコードが出なくなりました。
- 英語の「does not exceed 10 special」直訳をやめ、日本語で AnneHappy を「アンハッピー」と誤訳していた問題も修正しました。
### 2026年8月13日 Survivor MVP 翻訳
- `survivor_mvp.smx` を 0.3.4 へ更新。ラウンド MVP/LVP、個人順位、BOT 接頭辞の中国語ハードコードをやめ、クライアント言語で出力します。英語プレイヤーに「特感/小僵尸/友伤/[机器人]」が出なくなりました。
- 日/韓/越で「特感・黒槍・薬役」を「特殊感覚・黒い銃・医薬品」と誤訳していた問題を修正し、スペイン語に欠けていた Tag と Anne 簡易統計キーを補いました。
### 2026年8月13日 l4d_stats チャット i18n
- `l4d_stats.smx` を 1.5.6 へ更新。チャットに残っていたハードコード英語を翻訳フレーズへ移し、簡中/繁中/英/日/韓/越の 315 フレーズを揃えました。言語別ファイルは地図記録の 13 件だけではありません。
- 特殊感染者キル、Smoker/Hunter 救助、Charger 救助、Tank 岩ヒットの告知は閲覧者の言語でクラス名を出します。人間プレイヤーはニックネームのままです。
- マップ記録のチーム名フォールバックとコンソール `[RANK]` 接頭辞も翻訳します。`global_chat.smx` を 1.1.1 へ更新し、全サーバーのマップ記録放送も同じチーム名翻訳を使います。
### 2026年8月13日 Anne Tank 出現通知の翻訳
- `optional/AnneHappy/l4d2_tank_announce.smx` を 1.0.1.1 へ更新。チャット、Hint、中央文字をすべて i18n 化し、プレイヤー/AI 接頭辞はクライアント言語で組み立てるため、英語プレイヤーに中国語の「(玩家)」が出なくなりました。
- 英語の中国語直訳（「has been generated / controller」）をやめ、Hint フレーズを追加。スペイン語に欠けていた Anne 専用キーも補いました。
- 対戦版 `Spawned` の日/韓/越/繁中の表現も揃え、Tank は固有名詞のままにしています。
### 2026年8月13日 水中減速チャット翻訳
- `l4d2_slowdown_control` の Tank 出現中／撃破後の水中減速チャットを修正。英語の中国語直訳（「tank generation」など）、ベトナム語の Tank を「xe tăng」と誤訳する問題、日本語のプラグイン名の不一致を解消しました。
- 簡体中文、繁体中文、英語、日本語、韓国語、ベトナム語を上流の意味に揃えました。Tank 出現中は水中減速が弱まり、Tank がいなくなると通常に戻ります。

### 2026年8月13日 server_name の AI 難度タグ残留
- `server_name.smx` を 1.4.8 へ更新。`[AI:専門家]` のような部屋名タグは、`annehappy_dynamic_ai_difficulty.smx` が実際にロードされ、かつ档位が決まっているときだけ表示します。
- 動的難度プラグインをアンロードしても SourceMod は `ah_ai_dynamic_current_level` を残します。旧処理は残留した 1-6 档を `hostname` / `sn_main_name` へ書き続けていました。いまはプラグインが動作中かを確認し、すでに部屋名へ書き込まれた `[AI:...]` も取り除きます。
- `annehappy_dynamic_ai_difficulty.smx` を 2026.08.13.1 へ更新。`annehappy_dynamic_ai_difficulty` ライブラリを登録し、アンロード時に `current_level` / `current_mode` / `current_ppm` / `current_locked` を 0 に戻します。HUD、`!xx`、スポーン戦略が古い档位を読み続けないようにします。

### 2026年8月13日 Scripted HUD カスタムスロットと白枠対策
- `extend/l4d2_scripted_hud.smx` を 1.2.0 へ更新。HUD 3 / HUD 4 でプレイヤーが表示内容を選べるようになり、`!hudmenu` に「HUD 3/4 表示内容」サブメニューを追加（サーバー既定 / 生存者状態 / 特殊感染者のHP / キル統計 / プレイヤーのPing）。選択は Cookie と `scripted_hud_prefs` データベースに保存され（旧テーブルは自動で ALTER 移行）、サーバーをまたいで保持されます。
- 特殊感染者HPページは生存 SI 数/上限、各小型 SI の現在HP、Tank の現在/最大HP、Witch 数を表示。キル統計は生存者ごとのラウンド内 SI/雑魚キルを集計し、Ping ページは人間プレイヤーの遅延を一覧します（生存者優先）。文言はすべて翻訳フレーズ（簡中/繁中/英/日/韓/越）です。
- hudmenu 周りで時々出ていた「白い縁取りの空の長方形」への対策：あれは EMS スクリプト HUD スロットが一時的に「可視だが NOBG/TEXT なし」のフラグ状態で描いた空の背景パネルです。クライアント別フラグをサーバーの正規フラグから再構築するようにし、ResetHUD のリフレッシュ窓でフラグ 0 が配信されることをなくしました。加えて両 HUD プラグインが使わない EMS スロット 4-7 をマップ開始時に明示的に非表示へ。
- パネルの高さをクライアントごとの実際の行数で個別化（m_fScriptedHUDHeight の sendproxy を追加）し、複数行のカスタム内容が切れなくなりました。`l4d2_scripted_hud_hud3_team` の既定値を 2（感染者のみ）から 0（全員）へ変更し、生存者が選んだ HUD 3 の内容が実際に見えるようにしました。
- HUD 2 に「全滅回数」フィールドを追加：`server.smx` が公開する `anne_round_wipe_count`（現在マップの全滅数、マップ移動でリセット）を読み取り、1 回以上全滅すると HUD 2 の末尾に `[全滅xN]` を表示します。HUD のみで、部屋名には書き込みません。`!hudmenu` →「HUD 2 表示フィールド」で個別に切り替え可能。HUD 2 設定を保存済みのプレイヤーは既定でオフのため一度オンにする必要があり、新規プレイヤーは既定でオンです。

### 2026年8月13日 Scripted HUD 追加コンテンツ
- `extend/l4d2_scripted_hud.smx` を 1.3.0 へ更新。HUD 3 / HUD 4 の表示内容に 6 項目を追加し、スロットごとに選んで Cookie/データベースへ保存できます。
- Tank ダメージ：この Tank 戦の与ダメージ上位 4 人（割合付き）。撃破後も次の Tank まで残し、不在時は「Tank なし」。
- チームアイテム：生存者ごとに投擲/回復/一時アイテムを圧縮表示。
- 次の特殊感染：`infected_control` の `GetNextSpawnTime` を読み取り（未ロード時は --）、場の小型 SI 数/上限も表示。
- 速度/連跳：自分（観戦中は視点先）の水平速度と今回の連跳ピーク。接地して歩き始めて約 0.25 秒でピークをリセット。
- ラウンド時間+ホード：`round_start` からの mm:ss。`create_panic_event` の 60 秒窓では「発生中」、それ以外はディレクターのホード倒計時。
- Witch 距離警告：最も近い Witch のメートル距離と状態（静/近/警戒/激怒）。15 m 以内は「近」。複数体は残り数を併記。新フレーズは 6 言語。

### 2026年8月13日 Scripted HUD 15 スロット開放
- `extend/l4d2_scripted_hud.smx` を 1.4.0 へ更新。EMS の 15 スロットすべてを `!hudmenu` →「HUDスロット15個すべて」で表示内容に割り当て可能（サーバー既定 / 未使用 / 生存者状態 / SI HP / キル / Ping / Tank ダメージ / チームアイテム / 次ウェーブ / 速度 / ラウンド時間 / Witch 警告 / 進行タンクWitch / サーバー状態）。
- HUD 1–4 の既定動作は維持。スロット 5–8（エンジン 4–7）は選ぶまで非表示で、選ぶと左列に出る。スロット 9–15（エンジン 8–14）は既定でエンジンのキル一覧を透過し、内容を割り当てたときだけそのキル行を上書きして右下へ移す（クロスヘアを避けます）。
- 設定は Cookie と `scripted_hud_prefs` に保存（`hud_mask` を smallint へ拡張、`slot_sources` 追加。旧 6 フィールド Cookie と hud3/hud4 列は引き続き読める）。メニューとスロット名は 6 言語。
- 1.4.1：スロット座標を画面比に合わせて配置。`!hudmenu` →「画面レイアウト」で 16:9 / 16:10 / 4:3 / 21:9 を選択。追加スロットは左列 4–9・右列 10–14 で、HUD1–4・キルフィード・クロスヘア・武器欄を避ける間隔。4:3 は右端切れ防止、21:9 は中央の 16:9 安全領域へ。SendProxy でプレイヤーごとに適用。
- 1.4.2：特感 HP・次ウェーブ倒計時・次ウェーブ隊列、および HUD2 の「特感数/間隔」は観戦と特感だけに表示し、生存者はそのスロット全体を隠す。メニューは「（観戦/特感）」と注記。新内容「次ウェーブ隊列」は `infected_control` の `InfectedControl_GetSpawnQueue` を読む（旧プラグインは --）。同じ列の追加スロットは実行数で下に積み、追加スロットは最大 4 行、HUD3/4 は最大 6 行で長文の重なりを防ぐ。
### 2026年8月13日 スポーン分散と職業多様性
- `infected_control.smx` を 2026-08-13.11 へ更新。Hunter / Jockey / Charger の後方生成をもう一段締めます。ハード拒否 **-5→-4**（`inf_score_behind_reject_gap_hjc`）、後方減点倍率 **2.0→2.5**、Flow 後アンカー **-4/-3**。Hunter の rear は 0.15→0.05、Jockey は rear を使いません。同じ進捗の候補は引き続き採点します。Smoker / Spitter / Boomer とウェーブ時序は変わりません。
- `infected_control.smx` を 2026-08-13.10 へ更新。Hunter / Jockey / Charger が生存者後方に出る確率をさらに下げます。この3種の後方ハード拒否は全局の -8 から **-5**（`inf_score_behind_reject_gap_hjc`）へ、後方減点は `inf_score_behind_hjc_scale`（既定 **2.0**）倍。Flow 後アンカーは -10/-9 から **-5/-4** へ。同じ進捗の候補は引き続き採点します。Hunter の戦術品質は rear を減らし側面/端を優先、Jockey の rear 比重は 0.15→0.05。Smoker / Spitter / Boomer の全局後方ルールは変更しません。ウェーブ時序は変わりません。
- `infected_control.smx` を 2026-08-13.9 へ更新。ハード分散をもう一段緩めます。`inf_spawn_sep_radius` 150→**120**、`inf_spawn_sep_ttl` 2.5→**1.5**。同一 Nav クールダウン `NAV_CD_SECS` は **1.0** 秒のまま。ウェーブ時序は変わりません。
- `infected_control.smx` を 2026-08-13.8 へ更新。Hunter の飛びつき準備距離を少し外へ。優先帯を 350–750 から **350–850** へ、甘口を環の 38%（約 500）から 48%（約 **590**）へ。中距離のまま、旧 950 の遠点には戻しません。Charger / Jockey は変更なし。ウェーブ時序は変わりません。
- `infected_control.smx` を 2026-08-13.7 へ更新。Charger / Jockey / Hunter が遠すぎる地点に落ちやすい問題を抑えます。Hunter の優先帯を 350–950 から **350–750** へ縮め、距離の甘口を環の 45% から 38%（約 500）へ、高所ソート 0.90→0.95、高所距離補償 800→450 とし、遠い高台を「中距離」に換算しにくくします。Jockey は **250–550**、Charger は **250–600**、甘口をやや手前へ。分散ウェイトは 1.15/1.00→0.75/0.65。全体のハード分散半径 180→150、カーネル 40→30、採点枠は近/中/遠 6/4/2 から **6/5/1**。ウェーブ時序は変わりません。
- `infected_control.smx` を 2026-08-13.6 へ更新。距離帯を 10 段階から **6 段階** に戻します（stage 0 は優先帯のまま、stage 1–5 は優先上限と最終上限の間を線形補間）。10 段階では 1 環が数百単位しかなく近/中/遠を分ける意味が薄く、6 段階の方が三分位に距離の意味が出ます。近/中/遠は現在ページの min/max ではなく、**現在帯の検索環**（`scanMin..maxRange`）を三等分します。セクター配分は前 2/3（0–3）のみ制限、後ろ 1/3（4–5）は対象外。ウェーブ時序は変わりません。
- `infected_control.smx` を 2026-08-13.5 へ更新。採点枠は「この tick で先に合格した 8 点」ではなく、現在の候補ページを経路距離の三分位に分け **近 6 / 中 4 / 遠 2**（合計 12）で採点します。消費順は 3:2:1、枠が埋まった三分位は高コストの可視/経路判定を飛ばし、空き枠は他の三分位へ溢れさせて出特を遅らせません。`inf_spawn_nav_expensive_per_slice` の既定は 24（旧 16）。CVar：`inf_spawn_budget_near/mid/far`（すべて 0 で三分位オフ）。ranked 高所チャンネルはグループ内順を保ち、三分位の入れ替えのみ行います。ウェーブ時序は変わりません。
- `infected_control.smx` を 2026-08-13.4 へ更新。1 ウェーブの特殊感染者が狭い一角に固まる問題に対して分散処理を作り直し、職業構成に制御されたランダム性を導入しました。ウェーブ時序契約（キルフェーズ、独立した 16 秒基礎カウントダウン、Anti-Bait）は変わりません。
- 距離帯を 6 段階から 10 段階へ細分化：各職業の優先帯（stage 0、例 Smoker 500–1200）は分割せず維持し、スポーンが平均的に近くならないようにしています。失敗時の拡張は優先上限と最終上限の間を線形補間（Smoker は 1 段階ごとに +200、最大 3000）し、1 回の昇段で走査する環が小さくなり距離の上昇がより滑らかになり、1 つの環がシャッフル済みの 1 ページに収まりやすくなります。セクター配分は前 2/3 の帯のみ制限し、後ろ 1/3 は対象外に変更しました。
- 有向候補ページの取得後にページ内シャッフル（`inf_spawn_candidate_shuffle`、既定オン）：旧実装は経路距離の昇順で候補を消費し、1 回の試行で最大 8 個の合格点しか受け付けないため、常に「最も近い一塊」からしか選べませんでした。シャッフル後は採点対象が距離帯全体に広がります。ranked 高所チャンネルは並び順の利点を保つためシャッフルしません。
- ハード分散を CVar 化して強化：`inf_spawn_sep_radius`（既定 180、旧ハードコード 80）と `inf_spawn_sep_ttl`（既定 2.5 秒、旧 0.5 秒）。特殊上限によるスケーリングは維持。
- 分散スコアに連続カーネル減点を追加：生存中の特殊感染者と直近スポーン地点に対して exp(-d/r) を累積して減点（`inf_spawn_kernel_radius` 既定 280、`inf_spawn_kernel_points` 既定 40）。「直近 3 セクターだけ覚える」粗い記憶を補完し、セクター減点は維持します。
- ウェーブ内セクター配分を新設：1 ウェーブ・1 セクターの着地数 ≤ ceil(特殊上限/セクター数)+`inf_spawn_sector_quota_bonus`（既定 +1、-1 で無効）。制約は近い 4 つの距離帯のみで、遠帯とディレクター代替は対象外のため、細長いマップでもスポーンが止まりません。
- 高所優先は拡張の ranked 有向候補へ移行：`inf_nav_high_sort_scale` を再有効化（新既定 Smoker 0.85 / Hunter 0.90）。ターゲットより `inf_nav_high_sort_min_height`（既定 64）以上高い候補は割引距離で前倒しされ、限られた採点枠に入ります。地面近くの塊の中で事後加点に頼る必要がなくなりました。
- 職業多様性：職業選択の希少度に ±`inf_class_scarcity_jitter`（既定 0.06）の小さなランダム揺らぎを加え、占有率が近い職業間の固定ローテーションを崩します。支援ゲート（Boomer/Spitter は圧制職の後）はウェーブごとに確率 `inf_support_gate_waive_prob`（既定 0.15）で緩和。Breach ウェーブは緩和しません。
- `[SCORE-CHOSEN]` デバッグログと `sm_navdebug` の採点内訳にカーネル項（kern）を追加。

### 2026年8月14日 ネットワーク品質レポートはロス優先
- `network_quality_hint.smx` を 1.1.5 へ更新。ウェブサイトの異常イベントはパケットロスを優先します。`choke` が `nqh_report_choke_limit`（既定 20%）以下なら異常として報告しません。ゲーム内チャット警告は引き続き `nqh_choke_limit`（既定 5%）を使います。
- 異常理由：タイムアウトは `timing_out`。それ以外は `loss` を優先し、次に `ping`、しきい値を超えた `choke` です。

### 2026年8月14日 プレイヤー向けテキストの六言語揃え
- `!xx` 状態行の末尾を「バージョン」に変更し、「電信回線」との誤記をやめました。英語の Tank stall / HP return と、日本語のエキスパート・連続ジャンプも源码の意味に合わせています。
- トレイターと Anti-Bait の提示は、停滞（キャンプ）、ホールド中、ウェーブ解放を分けます。日/韓/越の英語残りを補い、トレイター訳名を統一しました。
- 既定の Scripted HUD 生存者状態はクライアント言語で組み立て、見出しを「蘇生回数」にしました。
- 白黒 Hint はフレーズ化し、次のダウンで即死と明記。`!ht` メニューに日/韓/越を追加。技能告知の中国語クラス名をトレイター/統計と揃えました。

### 2026年8月15日 Actions クラッシュ修正
- `actions.ext` が Action 破棄時に存在しない user-data キーを再挿入し、3組の Action ポインタキャッシュを削除しない問題を修正しました。Linux 拡張は `3.7.6-anne.2` となり、破棄時に user-data、identity-data、actor キャッシュを削除します。
- リスナーが存在しない場合でも Action ごとに空の pre/post リスナーハッシュを作成し、破棄後も外側の Action ポインタキーを保持していた問題を修正しました。候補 Action の pending マーカーも削除します。
- `ai_charger3` を 1.0.1.7 に更新しました。`ChargerEvade` の `CommandABot(MOVE)` は次フレームへ遅延し、Actions の `OnUpdate` コールバック内で NextBot 行動ツリーを同期的に再構築しません。

### 2026年8月15日 新規プレイヤーのトレイター状態残留を修正
- `infected_control.smx` を 2026-08-15.1 へ更新。プレイヤーがサーバーへ入る際、使用する client slot のトレイター登録を明示的に消去します。前の利用者の切断処理が完了していなくても、新規プレイヤーは非トレイターとして開始し、感染者チームへ誤って割り当てられた場合は `join.smx` が観戦へ戻せます。その後の通常のトレイター登録には影響しません。

### 2026年8月15日 Scripted HUD の次ウェーブ状態を修正
- `infected_control.smx` を 2026-08-15.2 へ更新し、撃破フェーズ、独立した基礎カウントダウン、Anti-Bait ホールドを区別するウェーブ状態 native を追加しました。従来の ETA native との互換性は維持します。
- Scripted HUD を 1.4.4 へ更新。正確な秒数は基礎カウントダウン中だけ表示し、撃破フェーズと Anti-Bait ホールドは明確な多言語状態を表示します。状態 API がない 2026-07 以前では、約8秒ずれる推定値の代わりに `--` を表示します。

### 2026年8月15日 Scripted HUD に CS キル HUD を統合
- Scripted HUD を 1.5.0 へ更新しました。独立した `l4d2_cs_kill_hud.smx` と旧キル HUD 投票を削除し、CS 風キルフィードを内蔵しました。各プレイヤーは既定で無効で、`!hudmenu` から個別に切り替えられます。サーバー全体の有効化 CVar は使いません。
- 有効にしたプレイヤーにだけ SendProxy でエンジンスロット 8–14（メニュー上は HUD 9–15）を上書きし、そのプレイヤーの設定メニューから隠します。無効のプレイヤーは標準キル一覧を透過し、自分のスロット設定を保持します。
- `!hudmenu` を CS キルフィード、基本 HUD（1–4）、追加 HUD（5–8）、拡張 HUD（9–15）、画面レイアウト、HUD 2 項目のグループ構成に整理しました。キルフィードとスロット設定はプレイヤーごとに保存されます。

### 2026年8月15日 死体溶解投票を削除
- 各モードメニューから「死体を溶解」の投票を削除し、`cfg/vote/dissolve_on.cfg` と `cfg/vote/dissolve_off.cfg` も削除しました。

### 2026年8月19日 低体力・医療キットなしで Anti-Bait を停止
- `infected_control.smx` を 2026-08-19.1 へ更新。立っている生存者の実体力が全員 `inf_antibait_lowhp`（既定 40）未満で、生存者（ダウン含む）が医療キット／除細動器／痛み止め／アドレナリンを誰も持っていないとき、Anti-Bait 全体を `Paused` にします。ダウン体力は実体力ではありません。閾値 0 で無効化できます。

### 2026年8月19日 Scripted HUD が生存者向け「特感数/間隔」を復帰
- Scripted HUD を 1.5.1 へ更新。1.4.2 は HUD2 の「特殊感染者数/間隔」を、特殊感染者HP・次ウェーブ秒数・スポーンキューと同じ観戦/感染者向け情報として隠していました。`[N特M秒]` はホスト名と同じ公開ルーム設定であり、場の情報ではありません。生存者にも再表示し、内蔵 CS キルフィードと共存します。キルフィードは引き続きそのプレイヤーのエンジンスロット 8–14 だけを使います。
- 特殊感染者HP、次ウェーブ秒数、次ウェーブキューは観戦/感染者のみのままです。

### 2026年8月20日 ヒットフィードバックの「チンチン」セットを旧 killsound に近づける
- `l4d2_hitsound.smx` を 2.6.0 へ更新。サウンドセットの任意キーは 3 つに整理：`headshot_kill`（ヘッドショットキル専用音）、`stack`（同フレーム重ね/まとめ）、`apply_special_only`（選択時に範囲を書き込む）。キルは死亡イベントで 1 音だけ。再生は常に `SNDCHAN_AUTO`。火・燃焼弾・`DMG_DIRECT`・爆発・`weapon_id==0` のフィルタは変更しません。
- サウンドセットはヘッドショット、ヒット、キル、ヘッドショットキルの4項目を一括設定します。ヘッドショットキルは `0..N` から個別に選択でき（0=通常のヘッドショット音）、そのIDと再生モードを RPG テーブルに保存し、旧 `SoundSelect.txt` から一度だけ移行します。重複するオーバーレイクイックトグルと `sm_hitui` は削除し、アイコンセット0を無効化に使います。
- セット 9「元のチン投票プラグイン」：ヘッドショット命中はチン、ヘッドショットキルはベル、それ以外は無音。選択すると範囲を全感染者にします。

### 2026年8月23日 ヒットフィードバックの個別音源を再設計
- `l4d2_hitsound.smx` を 2.7.0 へ更新。サウンドセットは実在する音だけを適用する「プリセット」になり、空の項目は自動で無効になります。セット 9 はヘッドショット音と専用ヘッドショットキル音だけを有効にし、ヒット/キルを有効とは表示しません。
- プレイヤーはヘッドショット、ヒット、キル、ヘッドショットキルの音源を個別に選択できます。各メニューは該当音を持つ音源だけを表示し、選択後すぐ試聴します。ヘッドショットキルの `0` は通常ヘッドショット音への追従です。
- ダメージ前に発火する `infected_hurt` の致死判定、管理者の単項目無効化が3項目を消す問題、DB初回ロード前の上書き、認証順序、高速連続保存の逆転、動的マスタースイッチを修正。既存DB/KV値はロード時に正規化されます。今回の再設計で列は増えませんが、`hitsound_stack_mode` / `hitsound_headkill` が未追加のDBでは、既存の冪等マイグレーション `20260820_hitsound_personal_prefs.sql` を一度実行してください。

### 2026年8月23日 共通サウンドカタログと全イベント自由選択
- `l4d2_hitsound.smx` を 2.8.0 へ更新。プリセット内の空でない25参照を実パスで重複排除し、安定ID `S01..S22` の22音にしました。プリセット1/3で重複する3音は一度だけ保持し、ダウンロードとプリキャッシュも各音一回です。
- ヘッドショット、ヒット、キル、ヘッドショットキルの全メニューが同じ共通カタログを表示し、どの音も任意のイベントへ割り当てて即時試聴できます。プリセット9の表示は `[頭:S21 中:無 殺:無 爆殺:S22]` です。
- 既存DB/KVの正数プリセット値は移行不要です。新しい共通選択は `-1..-127` を使うため4列は符号付き整数である必要があります。リポジトリ既定schemaは対応済みで、`20260823_hitsound_sound_catalog.sql` は誤って `UNSIGNED` で作られた表だけを冪等に修正します。

### 2026年8月23日 帽子設定の二層永続化
- `l4d_hats.smx` を 1.47.0 へ更新。帽子種類、着用、1人称で自分を見る、3人称で自分を見る、他プレイヤーの帽子表示の5項目を clientprefs Cookie に保存し、`l4d_hats_save 0` でも読み込みます。
- `rpg.smx` を 2.1.0 へ更新。`databases.cfg` に `rpg` MySQL がある場合は4つの切り替えを `RPG` テーブル優先で保存し、MySQLが使えない場合や列移行に失敗した場合は clientprefs SQLite にフォールバックします。既存アカウントの `NULL` 列は Cookie 準備完了後だけ移行し、既定値による上書きを防ぎます。
- ラウンドで再作成された帽子にも観戦者別 `SetTransmit` フィルターを常時設定するため、他人の帽子を隠す設定が維持されます。冪等マイグレーション `20260823_hat_preferences.sql` を追加し、プラグインから同じ列を自動追加することもできます。

### 2026年8月20日 Tank 頭乗り判定をモデル埋め込みに対応
- `ai_tank3` を 1.0.0.10 へ更新。騎乗の第一証拠は引き続き `m_hGroundEntity == Tank` で、隣接する高台や階段を騎乗と見なしません。
- 地面エンティティが Tank でない場合は「水平の衝突投影が重なる + 足元から下向き hull が実際に Tank に当たる」を要求します。モデルに埋まり地面が world のままの騎乗を拾い、壁に当たらない中心レイを証拠にしません。

### 2026年8月19日 Tank の頭乗りと梯子落下
- `ai_tank3` を 1.0.0.9 へ更新。頭上ブロックを「Tank の衝突体の上に立っている」騎乗と、届かない高台停滞の 2 種類に分けました。
- 騎乗は 2 秒の停止待ちもブラックリスト切替もしません。パンチ時に騎乗者へ上向き `SweepFist` を追加し、3 秒経っても頭上ならその場で強制投石します。250 離れる必要はありません。近距離の石は目標へ直接狙い、水平距離が 0 に近いときの弾道除算を避けます。騎乗中はジャンプ投石しません。
- 高台停滞は従来の 2 秒移動ウィンドウです。他の生存者がいれば切替（立っている → ピン留め → ダウン）。代替がいなければその場で投石し、同じ届かない目標へ `CommandABot RESET` しません。これが同じ梯子を登って落ちる主因の一つでした。梯子付近でも RESET しません。
- 梯子上ではピッチを水平に戻し、`m_climbableNormal` でヨーを梯子へロックして、バニラの「登りながら被害者を見て落ちる」循環を減らします。

### 2026年8月29日 コントロール特感の独立ターゲット上限
- `SI_Target_limit.smx` を 1.7 へ更新。Smoker / Hunter / Jockey / Charger が共通のロック上限を持ち、計算式は「コントロール特感予算 / 行動可能な生存者 + 1」。予算は有効なコントロール職の `z_*` / `inf_*` 上限の合計で、`l4d_infected_limit` で打ち止め。Boomer / Spitter / Tank はこの枠を使いません。
- `l4d_target_override.smx` を 2.26 へ更新し `targeted_mask` を追加。カウント対象はコントロール特感のみ。先頭フロー ×2 とランナー解除の意味は従来どおりです。
- Anne 各モードの `shared_settings.cfg` に `SI_enable_option 53` を明示しました。
- `l4d_target_override.smx` を 2.27 へ更新：`L4D_TargetOverride_SetLastVictim` を追加し、`L4D2_OnChooseVictim` 経由でターゲットを書き換える AI プラグインも同じロック台帳に登録できるようにしました。`L4D_OnTargetOverride` forward の書き換えが台帳に反映されない問題も修正。
- `SI_Target_limit.smx` を 1.8 へ更新：`IsClassTargetLimited` native と管理者向けデバッグコマンド `sm_targetlimit_status` を追加。1.7 の更新履歴コメントが早期終端しコンパイルできなかった問題を修正。
- 特感 AI の台帳統一：`ai_hunter_2` / `ai_jockey_2` / `ai_charger3` はターゲット変更前にコントロール系上限を尊重（新ターゲットが満杯なら変更しない）、`ai_spitter_3` / `ai_boomer_3` / `ai_tank3` は変更後に台帳へ登録。`ai_smoker3` はターゲットを書き換えないため対象外です。
- 人数自動適応：4 人、5〜8 人マルチ、ソロ残局のいずれでも式が自動スケールします。総容量は常に「コントロール系予算 + 行動可能な生存者数」で、場のコントロール特感数を必ず上回ります。生存者が少ない場合は上限が自動的に緩和され、特感が合法ターゲットを失うことはありません。
- バージョンロールバック設定 `Anne25-11` / `Anne25-10` / `Anne24-5` / `Anne23-1` に `SI_Target_limit.smx` のロードを追加（従来は `unloadall.cfg` でアンロード後に再ロードされず、ターゲット上限が無効化されていました）。

### 2026年8月28日 Scripted HUD スロットのチーム別表示
- Scripted HUD を 1.6.0 へ更新。`!hudmenu` の各スロットに「表示チーム」サブメニューを追加し、生存者/観戦/感染者を任意に組み合わせられます（最低 1 つは選択）。例えば観戦中のみ表示するスロットが作れます。選択は Cookie とデータベースに保存され（`scripted_hud_prefs` に `slot_team_masks` 列を追加、自動移行）、既存の設定は全チーム表示のままです。
- 機密コンテンツソースのチーム許可はハードコードから cvar `l4d2_scripted_hud_source_teams` へ移行（`ソース:チームマスク` 形式、ビット 1=観戦 2=生存者 4=感染者）。既定値 `2:5,7:5,13:5` は旧動作と同じで、特感体力・次ウェーブカウントダウン・スポーンキューは観戦/感染者のみ。`13:1` のように設定すれば観戦専用に絞れます。メニューの「（観戦/特感）」タグは実際の設定から動的に生成され、6 言語の翻訳も追加済みです。
- チーム制限で非表示のスロットはテキスト自体をクライアントへ送信しなくなりました（従来は描画しないだけで文字列はネットワークを流れていました）。チーム変更時は即座に表示を更新します。

### 2026年8月29日 AI Jockey 突き飛ばし後抑制の修正
- `ai_jockey_2.smx` を 2026.08.29 へ更新。「Jockey を突き飛ばしたその一撃が逆に懲罰分岐を武装させる」抜け穴を修正：生存者の直近のスイングが Jockey 自身を突き飛ばしたものである場合、「シャブ CD 中に即座に飛びつく」懲罰は発動しません。本当に無駄撃ちしたシャブ（空振り、他の特感への使用）に対する懲罰は従来どおりです。
- 突き飛ばし後の抑制時間は `z_jockey_leap_time`（Anne 設定では 0.4 秒）を流用せず、新設の独立 cvar `ai_JockeyShovedCooldown`（既定 3.0 秒、対戦ルールで突き飛ばされた Jockey に適用される jumpcap ブロック標準時間に一致）で管理します。抑制中はプラグインによる bhop/飛びつき強化を一切行わず、ネイティブ AI の跳躍ボタンも抑止するため、0.9 秒のよろけが終わった瞬間に空中で再飛びつきすることはなくなりました。移動や後退は通常どおり可能です。jumpcap パッチの AI 除外とよろけ関連 cvar は変更していません。

### 2026年8月30日 Jockey 突き飛ばし後抑制を AI 難易度別に段階化
- `ai_JockeyShovedCooldown` を動的 AI 難易度システムに接続しました。`dynamic_ai_difficulty.cfg` の 6 段階でそれぞれ、簡単 `3.0`、普通 `2.5`、困難 `2.0`、エキスパート `1.5`、極限/音理 `1.0` 秒です。値は `annehappy_dynamic_ai_difficulty.smx` が段階ごとに適用し、段階切替時にベースラインへ復元するため残留しません。
- プラグイン既定値 `3.0` は簡単段階と同じ強度なので、動的難易度プラグインを実行しないモードは自動的に最も緩い設定になります。

### 2026年8月29日 AI 特感 path 連跳「空飛ぶ大ジャンプ」修正
- path 連跳（バニーホップ）で稀に 1 回のジャンプが異常に遠くへ飛ぶ、空を飛ぶように見える問題を修正しました。根因は 3 つ：`ai_spitter_3` / `ai_boomer_3` のジャンプ加速が「現在速度への加算」で上限が一切なく、長い直線では毎ジャンプ +100/+150 と雪だるま式に加速していたこと；共有の空中補正が記録したジャンプ速度を滞空中ずっと下限として維持し、壁に当たってエンジンに減速されてもピーク値へ戻していたこと；推進ベクトルに垂直成分が含まれ（Boomer は高所のパス地点へ、Spitter は視線ピッチに沿って）、ジャンプ自体の Z 速度と重なって文字どおり「空飛ぶ」状態になっていたことです。
- `ai_spitter_3` を 3.0.6 へ更新し `ai_SpitterBhopMaxSpeed` を新設、`ai_boomer_3` を 3.0.8 へ更新し `ai_BoomerBhopMaxSpeed` を新設。加算式の推進は水平速度で上限クランプされ、推進ベクトルは水平成分のみを保持（垂直の高さはジャンプ本体に任せる）、起跳前の安全チェックは実際に適用される速度と同じ速度でトレースします。
- 両方の新しい上限は「Charger 上限 +200」の規則に従います：プラグイン既定値は 1000（= `ai_charger3_bhop_max_speed` 既定 800 + 200）、動的 AI 難易度 `dynamic_ai_difficulty.cfg` の 6 段階には 600 / 600 / 700 / 800 / 1000 / 1000 を設定（Charger の各段階 400 / 400 / 500 / 600 / 800 / 800 に対応）。
- 共有モジュール `ai_path_movement.inc`：`AIPathMovement_BeginPathHop` に最大速度引数を追加。空中補正は各特感自身の連跳上限を超える速度を維持しなくなり、外力によるスパイクも滞空中に保持されません。
- `ai_smoker3` を 1.0.1.3、`ai_tank3` を 1.0.0.11 へ更新：起跳フレームも `ai_smoker3_bhop_max_speed` / `ai_tank3_bhop_max_speed` でクランプします（従来は空中フレームのみ制限で、起跳の瞬間は超過できました）。`ai_charger3` を 1.0.1.8 へ更新。地上/Direct 連跳には元々上限があり、今回その上限を共有空中補正に登録するようにしました。
- 再コンパイル：`ai_smoker3.smx`、`ai_boomer_3.smx`、`ai_spitter_3.smx`、`ai_tank3.smx`、`ai_charger3.smx`。Jockey は元々 1 ジャンプ 400+80 の上限があり、Hunter には連跳ロジックがないため変更不要です。

### 2026年8月30日 Tank 各難易度の連跳上限を引き下げ
- 動的 AI 難易度の `ai_tank3_bhop_max_speed` を各段階で 200 引き下げ、上限 800 でクランプ：簡単 500、普通 600、困難 700、専門 800、極限 800（850 が上限に到達）；音理段階は 1000 を維持。設定のみの変更（`dynamic_ai_difficulty.cfg`）で再コンパイル不要；プラグイン既定値（動的難易度を使わない場合）は 1000 のままです。

### 2026年8月30日 トレイター登録のキャンセル対応とトレイター Tank の廃止
- `infected_control.smx` を 2026-08-30 へ更新。`!itcancel`（`!neiguicancel` と同等）を新設：ウェーブ解放前ならいつでもトレイター登録をキャンセルできます。登録済みの状態で `!it` を開くとメニューには「登録キャンセル」項目のみ表示されます。キャンセルしたスロットを AI で即時補充することはなくなりました。解放ウィンドウが開いた後（SpawnAllowed）はキャンセルできず、「ウィンドウは閉じられました」と表示します。
- トレイタークラスから Tank を削除：選択可能クラスは現在のスポーンプールが決めるようになり、Tank は登録できなくなりました。`inf_traitor_class_mask` の既定値と上限は 127 のままです（bit 64 はロールバック互換のためだけに残しており、実際には効きません）。`z_max_player_zombies` がトレイター Tank 用に余分なスロットを予約することもなくなりました。
- 選択可能クラスは現在のスポーンプールとの積集合になりました：`inf_*_limit 0` で無効化されたクラスは登録不可、allcharger / allhunter モードではそれぞれ Charger / Hunter のみ登録できます。
- `inf_traitor_max_slots` の有効値は `l4d_infected_limit - 1` にクランプされ、毎ウェーブ最低 1 つの AI スポーン予算を確保します。トレイターの自動連跳は OnPlayerRunCmd チャンネルへ移行（内部リファクタリング、挙動は同じ）。usage とメニュー文言は六言語の翻訳を同期更新しました。
- バージョンロールバック設定 `Anne26-07` / `Anne25-11` / `Anne25-10` に `smart_ai_rock.smx` の読み込みを補いました（従来は `unloadall.cfg` がアンロードした後、ロールバック版では Tank のスマート投石が失われていました）。

### 2026年8月30日 Charger 銃所持の近距離は先に2撃してから突進
- `ai_charger3` を 1.0.1.9 へ更新。`ai_charger3_bhop_min_dist`（既定 100）に入った銃所持目標は命中確実とみなし、確率待ちをやめ、突進距離も 36–95 に圧縮しません。
- 爪撃距離内で2撃する間に目標が逃げ切れない場合は、先に2撃してから突進します。届かない、後退中、または 2.5 秒を超えた場合は即座に突進します。ショットガンは 100 以内で即突進のままです。近接武器との駆け引きは変更していません。
- 再コンパイル：`ai_charger3.smx`。

### 2026年8月30日 高SIの生成後排斥を緩和
- `infected_control.smx` を 2026-08-30.1 へ更新。高SIではウェーブ後半が「生成後の排斥」で近帯から押し出されていました。核密度がすでに生存者の眼前にいる特感まで占有源にし、しかも特感上限スケールを掛けていませんでした。硬分散のほうは上限に応じて縮むため、緩めるべき高SIでかえって厳しくなっていました。
- いずれかの生存者から 400 未満の生存特感は核密度源に入れません。特感上限スケールは減点値ではなく核半径（`inf_spawn_kernel_radius` × 係数）に移しました。低SIでは押し広げ、高SIでは重なり防止だけに効きます。曲線全体が平坦化して分解能を失うことがなくなります。`inf_spawn_kernel_points` の既定は 30 から 50 へ引き上げました。
- 扇区による分散軸は一式削除しました（`recentSectors`、ウェーブ内の扇区クォータ、`inf_spawn_sector_quota_bonus`）。方向の好みは連続的な戦術ジオメトリ点のみで表現し、分散点は核密度だけになりました。
- 硬分散は固定半径になり、特感上限にもウェーブ内占有にも連動しません。この判定は「ここに湧いてよいか」だけを答えます。`inf_spawn_sep_radius` の既定は 120 から 100 へ下げました。ウェーブ時序は変更していません。

### 2026年8月30日 AI の反応時間を全難易度で統一

- `dynamic_ai_difficulty.cfg` の `z_acquire_far_time` / `z_acquire_near_time` を、簡単〜エキスパートの 4 段階で `5.0 / 0.5` から `0.0 / 0.0` へ変更しました。もともと `0.0 / 0.0` だった極限 / 音理と揃います。ターゲット捕捉の遅延はもう難易度軸ではなく、段階差はすべて職業ごとの挙動 cvar で表現します。
- 設定のみの変更で再コンパイルは不要です。`docs/annehappy_dynamic_ai_difficulty.md` の「反応時間」行も同期しました。

### 2026年8月31日 コミット前レビュー修正

- `ai_charger3.smx` を再コンパイル。従来の成果物は 1.0.1.8 のままで、1.0.1.9 の「先に2撃してから突進」、銃所持向けの命中確実帯 `resolveChargeCommitDist`、制御系上限のターゲット変更ゲート、連跳上限の登録がいずれも入っていませんでした。すでにソースから削除済みの確率突進が動いたままでした。
- `ai_boomer_3` / `ai_spitter_3` の押し出しベクトル正規化のゼロ除算を修正。水平成分がゼロのとき（目標が真上・真下、視点ピッチ ±90）正規化は純粋な垂直ベクトルへ退化し、特感をそのまま空へ打ち上げます。今回直そうとしていた「空飛び」の別ルートそのものです。共有ヘルパー `AIPathMovement_NormalizeHorizontal()` を追加し、ゼロベクトルなら押し出しを行いません。
- `getChargerClawRange()` のキャッシュをマップ切り替えで失効させ、範囲外の値を読んだときはキャッシュしないようにしました。従来は関数内 `static` に永久キャッシュしていたため、武器スクリプト読み込み前に最初の呼び出しが起きると 70.0 のフォールバックがプロセス終了まで固定されていました。
- `spechud` の `FillTankInfo` を固定長配列に変更し、HUD 描画パスでパネルを組むたびに発生していた `new int[MaxClients]` のヒープ確保を削除しました。
- `sm_navtest` は対象生存者が使えないとき `GetTargetAnchorPos` にフォールバックするようにし、候補点と自分自身の高さを比べる（高さ点が常に 0 になる）状態を解消しました。あわせて未初期化の `targetEye` も修正しています。
- 特感 AI プラグイン 5 本で `#include <si_target_limit>` の後に `#define REQUIRE_PLUGIN` を戻し、`ai_tank3.sp` と揃えました。任意依存はこの 1 行の include だけに限定されます。
- `si_target_limit.inc` が `l4d_target_override` を推移的に include する際、呼び出し側の `REQUIRE_PLUGIN` 状態を保つようにしました。サードパーティのプラグインに硬依存が黙って追加されることはありません。
- 扇区システム削除後に `infected_control.sp` へ残っていた未参照マクロ 5 個を整理し、`PEN_LIMIT_SCALE_LO` のコメントを訂正しました。
- `.gitignore` の重複を整理し、`/test_results` ですでにカバーされる 9 行の個別パスを削除しました。
- `anne_server_redirect` の jp / ko / vi 翻訳を追加し、他の Anne プラグインと同じ六言語カバレッジに揃えました。
- ドキュメント訂正：`README_SPAWN_ARCHITECTURE.md` の分散度 2 項目、`annehappy_dynamic_ai_difficulty.md` の反応時間行・Tank 行・新設の連跳上限 cvar 2 つ、`ai_charger3/readme.md` で確率突進を説明したままだった 4 箇所。あわせて対比表の Charger 連跳速度も訂正しました（旧記載 45/60/75/90/150、実際の設定は 40/50/60/70/100）。

### 2026年9月1日 2026-08 AI バランス調整

- `ai_charger3` を 1.0.1.10 へ更新。Charger 専用 CVar `ai_charger3_air_speed_floor_ratio`（既定 0.50）を追加：Path / Direct 起跳時、空中方向補正に登録する hopSpeed を実際の起跳速度×この比率にし、`m_flLastHopSpeed` は実起跳速度のまま残します。
- 動的 AI 難易度 6 段階で Charger を弱体化：加速度 30/40/50/60/80/100、上限 400/400/400/500/700/700、空中補間 0.10/0.15/0.20/0.25/0.30/0.40、旋回減速 0.50/0.39/0.325/0.26/0.195/0.13、保速下限 0.50/0.60/0.65/0.70/0.80/0.80。簡単段階の減速は既存の硬上限 0.50 を使用します。
- `ai_spitter_3` を 3.0.7 へ更新。`ai_spitter3_air_spit`（既定 0）を追加：極限と音理だけ 1 で、AI Spitter の空中吐痰を許可。簡単〜エキスパートは空中吐痰を禁止し、地上吐痰はそのまま、プラグインがジャンプを付けません。プレイヤー Spitter は制限しません。
- Smoker の `tongue_fly_speed` は 6 段階すべて明示的に 1000（極限/音理は 1200 から引き下げ）。
- 再コンパイル：`ai_charger3.smx`、`ai_spitter_3.smx`。

### 2026年9月1日 Charger 爪コンボのキーと駆け引き修正
- `ai_charger3` を 1.0.1.11 へ更新。近距離の爪攻撃と拘束中の殴りは再び `IN_ATTACK2`（左クリックは突進能力）。以前の `IN_ATTACK` では最大 2.5 秒立ち尽くして一発も出ませんでした。
- 銃所持対象へのコンボは、相手が見ていない・Charger の体力が近接即死線より上・相手がリロード/ショブ CD 中でない場合だけ開始し、500 体力で銃の正面に立ち続けるのをやめます。
- 2 撃予測は 2 撃目の 0.85 秒だけを見るように変更。確定突進の下限は 110 で上限し、横移動速度 ≥ 150 のときは commit 距離を引き上げません。
- 爪攻撃カウントに有効な `m_flNextPrimaryAttack` 基準値の状態を追加し、初期値が 0、または爪武器が一時的に取得できない場合でも初撃を数え落とさないようにしました。爪距離属性の取得失敗はマップごとに間隔を空けて最大 3 回だけ再試行し、毎フレームの native 呼び出しと Left4DHooks のエラーログ連打を防ぎます。
- 再コンパイル：`ai_charger3.smx`。
