# 云5官图药品路线筛选实测（2026-09-24）

后续新版已完成 57 张官图全量复测：43 张启用补药的非救援图全部达到上限 4。详见[新版全量报告](pill_flow_cloud5_progress_full_2026-09-25.md)；本文保留旧方案的历史数据。

逐图结果见 `pill_flow_cloud5_2026-09-24.csv`。云5（`anne1`）在无人时临时隔离，加载当前工作树编译的 `confoglcompmod.smx`（仅增加临时测试启用命令），逐张载入 `c1`–`c14` 共 57 张官方地图。地图配置 `mapinfo.txt` 与工作树哈希一致。测试时药上限为 4、进度下限为 30%；同一次地图加载内先不删除药地预览绕路阈值 300、600、关闭（0），再按 300 与自动补药实际执行一次。预览之间会重新枚举实体，因而数值只是该次加载的顺序快照，不是多次随机开图的概率。

| 范围 | 300 | 600 | 关闭路线筛选 |
|---|---:|---:|---:|
| 43 张非救援图原生合格药为 0 | 11 | 10 | 1 |
| 原生合格药达到 4 | 13 | 16 | 21 |
| 被路线规则剔除的药点 | 71（29 图） | 50（22 图） | 0 |

实际按 300 加自动补药，32/43 张非救援图达到 4 瓶；11 张完全没有合格锚点，补药无法发生，最终仍为 0。`c5m1_waterfront` 在本轮枚举中原始药点为 0，其他 10 张在关闭路线筛选时均有药点。14 张救援关因 `confogl_pills_flow_finale=0` 主动跳过，不应当按“0 瓶”解读。

无锚点且最终为 0 的地图：`c2m4_barns`、`c3m3_shantytown`、`c5m1_waterfront`、`c5m3_cemetery`、`c5m4_quarter`、`c6m1_riverbank`、`c8m1_apartment`、`c10m3_ranchhouse`、`c11m2_offices`、`c11m4_terminal`、`c13m3_memorialbridge`。

原始测试结论：300 对部分官图过严，且单纯提高到 600 只能小幅改善；当时补药依赖合格原生药作锚点，11 张地图无法补药。下方记录后续方案，不应将本段的旧补药结果当作当前实现的结果。

首次采集因自动回合与探针竞态被废弃；本报告只使用修正后的完整 57 图数据。云5已恢复原图、原有 CVar 值、公开访问、原插件文件哈希与运行状态，临时探针已移除。临时插件注册的新 ConVar 在本次进程内可能仍可查询，但旧插件不会使用，重启后消失。此次只验证生成日志及补药实体数，未实际进图拾取；随机药点尚未做多轮重复采样。

## 后续改动与抽查

补药不再依赖原生药锚点：先按原有进度和绕路条件筛选原生药，再从带 `NAV_SPAWN_ESCAPE_ROUTE` 标记的导航区按 30%–100% 进度随机补足上限；起终点安全区、不可站立位置和已有药附近仍排除。候选区不再逐个调用寻路，避免 `c13m2_southpinestream` 的长时间主线程阻塞。

云5临时插件抽查：`c5m1_waterfront` 原生零药补 4 个，`c2m4_barns` 补 4 个，`c14m1_junkyard` 补 3 个，`c13m2_southpinestream` 补 3 个；均落在 30%–100%，总数达到上限 4。后两图各有 1 个筛选后保留的原生药。主路线标记在这三张导航采样图（`c2m1_highway`、`c5m1_waterfront`、`c13m2_southpinestream`）的目标进度范围内分别有 183、93、211 个候选导航区。此次只抽查四张图，**未重新全量复测 57 张官图**，也未实际进图拾取。云5已恢复原图、原插件和原有配置，临时二进制已撤回。
